
export const get_ultra_realestic_prompt = (description: string) => {
    return `Create a highly detailed, photo-realistic image that looks like a professional photograph. The image should be based on the following description: ${description}.`
}